#include <stdio.h>
#include <stdlib.h>
#include <socket.h>

    // docker run -it -v /Users/nikhilreddy/OSSHack/spx-data:/data --rm msftspeech/spx recognize --file record_out.wav > test.txt

int main(argc, argv) {

    // create a server socket
    // bind the socket to a port
    // listen for connections

    while (true) {


    }
}